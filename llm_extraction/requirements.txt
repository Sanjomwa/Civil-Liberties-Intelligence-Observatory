# llm_extraction/ — OTF-04 LLM extraction prototype
#
# Deliberately its own requirements file, parallel to Bruin/requirements.txt
# and streamlit/requirements.txt, since this component is deliberately
# outside both the Bruin DAG and the Streamlit app (see ADR-0009). Install
# with:
#     pip install -r llm_extraction/requirements.txt
#
# requests, python-dotenv, and google-cloud-bigquery are already top-level
# project dependencies (pyproject.toml); pinned again here so this
# directory installs standalone without requiring the top-level
# environment. openai is new to this project -- confirmed working live
# against gpt-5.4-mini at version 2.52.0 before this pin was set.

openai>=2.0.0,<3.0
requests>=2.31.0,<3.0
python-dotenv>=1.0.0,<2.0
google-cloud-bigquery>=3.20.0,<4.0
